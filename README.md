## rvcli: Companion CLI for Rvapi

Minimal by design Go CLI to generate or refresh JWT token used for Rvapi

### Final result

A `rvconfig.env` file will be created in directory where CLI is ran.

Expect contents:

```
USERNAME=
PASSWORD=
FULLNAME=
EMAIL=
HOST=

JWT_TOKEN=
```
